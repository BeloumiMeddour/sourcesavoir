BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Professeur] ADD [chargeMax] FLOAT(53) NOT NULL CONSTRAINT [Professeur_chargeMax_df] DEFAULT 25;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
